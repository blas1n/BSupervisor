name: Demo smoke

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  demo-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        run: npm install -g pnpm@10

      - name: Generate ephemeral demo secrets
        run: |
          {
            echo "DEMO_JWT_SECRET=$(openssl rand -hex 32)"
            echo "POSTGRES_PASSWORD=$(openssl rand -hex 16)"
            echo "DEBUG=false"
            echo "LOG_LEVEL=INFO"
          } > deploy/.env.demo

      - name: Boot demo stack
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          docker compose -p bsupervisor-demo-ci \
            -f deploy/docker-compose.demo.yml \
            --env-file deploy/.env.demo \
            up -d --build --wait

      - name: Run alembic migrations
        run: |
          docker compose -p bsupervisor-demo-ci \
            -f deploy/docker-compose.demo.yml \
            --env-file deploy/.env.demo \
            run --rm app python -m alembic upgrade head
          docker compose -p bsupervisor-demo-ci \
            -f deploy/docker-compose.demo.yml \
            --env-file deploy/.env.demo \
            restart app
          for _ in $(seq 1 30); do
            if curl -fsS -o /dev/null --max-time 2 http://localhost:9000/api/health; then
              echo "backend healthy"
              break
            fi
            sleep 2
          done

      - name: Frontend deps
        working-directory: frontend
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN || secrets.GITHUB_TOKEN }}
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        working-directory: frontend
        run: pnpm exec playwright install --with-deps chromium

      - name: Run demo smoke suite
        working-directory: frontend
        env:
          DEMO_E2E_BASE_URL: http://localhost:9000
          DEMO_E2E_API_URL: http://localhost:9000
        run: pnpm exec playwright test --grep @demo --project=chromium

      - name: Demo backend logs (on failure)
        if: failure()
        run: |
          docker compose -p bsupervisor-demo-ci -f deploy/docker-compose.demo.yml logs --tail=200 app
          docker compose -p bsupervisor-demo-ci -f deploy/docker-compose.demo.yml logs --tail=50 postgres

      - name: Tear down
        if: always()
        run: |
          docker compose -p bsupervisor-demo-ci -f deploy/docker-compose.demo.yml down -v

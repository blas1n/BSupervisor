# API base URL for cross-domain deployment (e.g., Vercel frontend + separate API server)
# Leave empty or omit to use relative "/api" path (works with vite proxy in dev)
VITE_API_URL=https://api-supervisor.bsvibe.dev/api

# Progress Log
Started: 2026-03-28T09:41:27Z

## TASK-001: Core models + config + Alembic setup — DONE
- Created 4 SQLAlchemy models: AuditEvent, AuditRule, CostRecord, DailyReport
- Base class with UUIDPrimaryKeyMixin and TimestampMixin for shared fields
- UUID primary keys, DateTime with timezone for timestamps
- Decimal(20,8) precision for cost_usd fields (CostRecord, DailyReport)
- JSON columns for metadata_json, condition, report_json
- Async database engine + session factory in models/database.py
- Updated alembic/env.py to use Base.metadata for autogenerate
- Tests: 10 passing (8 model tests + 2 config tests), 98.72% coverage
- Ruff check: all clean

## TASK-002: Event ingestion API — DONE
- Created POST /api/events endpoint in bsupervisor/api/events.py
- Pydantic schemas (EventRequest, EventResponse) in bsupervisor/api/schemas.py
- Validates required fields (agent_id, source, event_type, action, target), optional metadata/timestamp
- Stores events in audit_events table via async SQLAlchemy session
- Returns {event_id, allowed, reason} — stub rule engine always allows
- Wired events router into main.py app
- Updated tests/conftest.py to override get_session dependency for test DB isolation
- 6 new tests: valid event, optional fields, missing required (422), DB storage, timestamp handling
- Total: 16 tests passing, 97% coverage, ruff clean

## TASK-003: Rule engine — DONE
- Created bsupervisor/core/rule_engine.py with RuleEngine class and RuleResult dataclass
- Built-in rules:
  - block_sensitive_file_delete: blocks file_delete on *.env, *.key, *.pem
  - block_dangerous_shell: blocks shell_exec with sudo, rm -rf, chmod 777
  - cost_threshold_warning: warns when agent daily cost exceeds threshold (configurable, default $50)
- DB rules: loads enabled AuditRule records, matches condition JSON against event fields
  - Supports event_type, action, target_pattern (fnmatch glob), agent_id matching
  - Actions: block (deny), warn (allow + reason), log (allow silently)
- Wired RuleEngine into POST /api/events endpoint (replaces stub)
- 23 new tests covering: block, warn, allow, DB rules, disabled rules, API integration
- Total: 39 tests passing, 96% coverage, ruff clean

## TASK-004: Cost tracking — DONE
- Created bsupervisor/core/cost_tracker.py with CostTracker class
  - record_cost(): creates CostRecord with timestamp, commits, returns record
  - get_daily_cost(agent_id, date): aggregates cost_usd for agent on given date
  - get_daily_total(date): aggregates cost_usd across all agents on given date
  - Uses func.coalesce to return Decimal("0") when no records exist
- Added Pydantic schemas: CostRequest (with ge=0 validation on tokens), CostResponse
- Created POST /api/costs endpoint (201 status) in bsupervisor/api/costs.py
- Wired costs router into main.py
- 14 new tests: record creation, multi-record, daily cost filtering by agent/date, daily total, API valid/invalid/persistence/precision
- Total: 53 tests passing, 97% coverage, ruff clean

## TASK-005: Daily report generator — DONE
- Created bsupervisor/core/reporter.py with Reporter class
  - generate_daily_report(date): aggregates events, costs, persists DailyReport
  - to_markdown(report): renders human-readable Markdown summary
  - Queries: total events, blocked count, total cost, cost by agent, cost by model, top agents (top 10)
  - Date filtering via timestamp range (start/end of day UTC)
  - Uses func.coalesce for zero-safe cost aggregation
- Created bsupervisor/api/reports.py with GET /api/reports/daily?date=YYYY-MM-DD
  - Returns JSON with report_id, date, total_events, blocked_count, total_cost_usd, report_json, markdown
  - 422 for missing or invalid date parameter
- Wired reports router into main.py
- 13 new tests: empty day, events aggregation, cost aggregation, cost by agent/model, top agents, DB persistence, date filtering, markdown output, API endpoint (valid/missing/invalid date/empty)
- Total: 66 tests passing, 97% coverage, ruff clean

## TASK-006: API routes + health check + app wiring — DONE
- Created GET /api/status endpoint in bsupervisor/api/status.py
  - Returns today's total_events, blocked_count, total_cost (Decimal normalized)
  - Date filtering via UTC day boundaries
- Created rules CRUD router in bsupervisor/api/rules.py
  - GET /api/rules: list all rules ordered by name
  - POST /api/rules (201): create rule with validation (action must be block/warn/log)
  - PUT /api/rules/{id}: partial update, 404 if not found
  - DELETE /api/rules/{id} (204): delete rule, 404 if not found
  - 409 on duplicate rule name
- Added Pydantic schemas: RuleCreateRequest, RuleUpdateRequest, RuleResponse, StatusResponse
  - Action validation via model_post_init (block/warn/log only)
- Set up FastAPI lifespan in main.py: engine.dispose() on shutdown
- Wired all 5 routers: events, costs, reports, rules, status
- 16 new tests covering: status empty/populated, list/create/update/delete rules, validation errors, 404s, 409 duplicate, health check
- Total: 82 tests passing, 93% coverage, ruff clean

## REVIEW-001: Code review — fix all issues — DONE
- Ran full code review across all source and test files
- Fixed 3 issues:
  1. Security: Removed hardcoded JWT secret default ("change-me-in-production" → empty string) in config.py
  2. Security: Sanitized database URL in startup log (hide_password=True) in main.py
  3. Status code: POST /api/events now returns 201 instead of 200, updated all related tests
- Verified: Decimal used consistently for all money fields, structlog everywhere, pydantic-settings for config, async for all I/O, type hints on all public functions, no os.getenv usage
- Final: 82 tests passing, 93% coverage, ruff check clean
